#include <stdio.h>
#include <string.h>

int main() {
    char a[100],b[100];
    int site;
    scanf("%s",a);
    scanf("%s",b);
    int lentha=strlen(a);
    int lenthb=strlen(b);
    for(int i=0;i<lentha-lenthb;i++)
    {
        for(int j=i;j<i+lenthb;j++)
        {
            
        }
    }

    return 0;
}